README

In this folder you have different code for the data collection.

the init.py will run  the vrpn and the force torque ros node. Then it will save the target position and the force torque bias at horizontal to a csv file.
 it shoudl be run in the folder runingfolder/src as following : 
 python init.py <target#>

